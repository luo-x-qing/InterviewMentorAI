# InterviewMentorAI Python AI Backend Dependencies

# FastAPI 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# OpenAI 兼容接口（调用阿里云百炼 qwen3.5-omni-plus）
openai>=1.0.0

# 配置加载（app/core/config.py 依赖；未显式声明会导致 CI 收集崩溃）
pydantic-settings>=2.0.0

# HTTP 客户端
httpx>=0.25.0

# 环境变量管理
python-dotenv>=1.0.0

# RAG新增依赖（课程推荐轻量栈）
numpy==1.26
sqlite-vec==0.1.6
rank-bm25==0.2.2

# 中文分词
jieba>=0.42.1

# 文档转换（PDF/Word/HTML → Markdown）
pdfplumber>=0.10.0
python-docx>=1.0.0
beautifulsoup4>=4.12.0

# PDF 图片 OCR（离线，RapidOCR；onnxruntime 需 1.19.x，1.23+ 在本机缺 VC++ 运行库）
rapidocr-onnxruntime>=1.4.0
onnxruntime==1.19.2

# 重排序模型（Cross-Encoder Reranker）
sentence-transformers>=3.0.0

# Agentic RAG：LangChain 提供标准化组件（RagRetriever），LangGraph 编排工作流。
# 注意：这两个库下载在项目目录 backend_python/lib 下（非全局环境），
#       由 agentic_rag_service 模块顶部显式注入 sys.path 加载。
langchain>=1.0.0
langgraph>=1.0.0

# MCP 协议封装业务接口（进程内 FastMCP server + call_tool；v1 API，2.x 改名 MCPServer）
mcp>=1.20,<2

# 测试
pytest>=7.0.0
pytest-mock>=3.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.23.0